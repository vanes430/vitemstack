name: Build and Release

on:
  push:
    branches: [ "*" ]
    paths:
      - 'src/**'
      - 'pom.xml'
  pull_request:
    branches: [ "*" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
    - uses: actions/checkout@v4

    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'
        cache: maven

    - name: Get Version from POM
      id: get_version
      run: |
        VERSION=$(mvn help:evaluate -Dexpression=project.version -q -DforceStdout)
        echo "Detected version: $VERSION"
        echo "version=$VERSION" >> $GITHUB_OUTPUT

    - name: Build with Maven
      run: mvn clean install

    - name: Generate SHA256 Hashes
      run: |
        cd target
        touch sha256sum.txt
        for f in *.jar; do
          sha256sum "$f" >> sha256sum.txt
        done
        cat sha256sum.txt

    - name: Upload Artifacts
      uses: actions/upload-artifact@v4
      with:
        name: vitemstack-Jars
        path: |
          target/*.jar
          target/sha256sum.txt

    - name: Update Tag and Release
      if: github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'workflow_dispatch')
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        VERSION: ${{ steps.get_version.outputs.version }}
      run: |
        # Konfigurasi Git
        git config user.name "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"
        
        # Hapus tag lama jika ada (remote & local) untuk memindahkan 'pointer' ke commit ini
        git tag -d "$VERSION" || true
        git push origin ":refs/tags/$VERSION" || true
        
        # Buat tag baru di commit saat ini
        git tag -a "$VERSION" -m "Release $VERSION"
        git push origin "$VERSION"

    - name: Publish Release
      if: github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'workflow_dispatch')
      uses: softprops/action-gh-release@v1
      with:
        tag_name: ${{ steps.get_version.outputs.version }}
        name: Release ${{ steps.get_version.outputs.version }}
        files: |
          target/*.jar
          target/sha256sum.txt
        generate_release_notes: true
        prerelease: false
